import React from 'react'

import App from '@/App'
import ReactDOM from 'react-dom/client'

import './style/_boilerplate.css'
import '@fontsource-variable/jost'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
